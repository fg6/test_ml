def repo = "${REPO}_DVC"
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }  
        }
        stage('Repo check') {
            steps {
                echo "${repo}"
            }  
        }
      }
    }










